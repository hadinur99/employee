import { CurrencyIDR } from './currency-idr.pipe';

describe('CurrencyIDRPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyIDR();
    expect(pipe).toBeTruthy();
  });
});
